# Release Checklist

1. Run `python scripts/validate.py`.
2. Review `data/stats.json`.
3. Confirm README, license, citation, and Zenodo metadata.
4. Create a Git tag such as `v1.0.0`.
5. Create a GitHub release.
6. Archive the release on Zenodo.
